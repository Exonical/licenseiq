run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - gofmt
    - goimports
    - gosimple
    - unused
    - misspell

linters-settings:
  misspell:
    locale: US

issues:
  exclude-use-default: false
